@model IEnumerable<UserQualfClaim>

@{ ViewData["Title"] = "Index"; }

<h1>Index SuperAdmin View</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Verified)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExpiryDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Id)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Verified)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.ExpiryDate)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new {  id=item.Id/*.PrimaryKey */ }) |
        @Html.ActionLink("Details", "Details", new { id = item.Id /* id=item.PrimaryKey */ }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Id /* id=item.PrimaryKey */ })
    </td>
</tr>}
    </tbody>
</table>
